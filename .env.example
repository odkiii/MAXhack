DATABASE_URL="postgresql://postgres:postgres@localhost:5432/max_hack?schema=public"

# Токен: business.max.ru → Чат-боты → spring_cat4_bot → Интеграция → Получить токен
MAX_BOT_TOKEN="YOUR_TOKEN"
MAX_API_URL="https://platform-api.max.ru"

# Публичный HTTPS-URL вебхука (после деплоя на Vercel):
# MAX_WEBHOOK_URL="https://ваш-проект.vercel.app/api/max/webhook"
MAX_WEBHOOK_URL=""
MAX_WEBHOOK_SECRET=""

# Для Neon/Supabase на Vercel — pooled URL (с -pooler в хосте), если есть
# DATABASE_URL="postgresql://...@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"

# Если GET /me даёт 401 — попробуйте MAX_USE_BEARER=1
# MAX_USE_BEARER=1
